type Portfolio {
  id: ID!
  title: String
  company: String
  companyWebsite: String
  location: String
  jobTitle: String
  description: String
  startDate: String
  endDate: String
}

type Query {
  getPortfolios: [Portfolio]
  getPortfolio(id: ID!): Portfolio
  getUserPortfolios: [Portfolio]
}
